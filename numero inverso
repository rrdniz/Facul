#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int v[10], v2[10], i;
	
	for (i=0 ; i<10 ; i++){
	printf("digite seu numero\n");
	scanf("%d", &v[i]);
	}  
	for (i=9 ; i>-1 ; i--){
		printf("numeros inversos: %d\n", v[i]);
	}   
return 0;
}
